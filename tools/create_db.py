import studio
import sqlite3

def table_exists():
  # Check if the table exists fitst. 
  # If no records, bail successfully.
  # Otherwise, if records exist, throw an error.

def create_table():
  # Attempt to create the table.

def create_contacts():
  # Use the studio library to create the contact list.
